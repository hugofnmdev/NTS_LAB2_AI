
class BinTree:

    """
    Tree that will contain couples (elem, list)
    where elem will be a word (or a special token)
    and list the list of possible successors
    """
    
    def __init__(self,key,left,right):
        self.key = key
        self.left = left
        self.right = right
        
def insert(B,x):
    """
    @param x : element
    inserts a value in the BST"
    """
    
    
def search(B,x):
    """
    @param x : element
    @return the node where the element is, None if x is not in the BST
    """
    pass
